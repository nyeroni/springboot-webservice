spring:
  profiles:
    include:
      - oauth
      - real-db

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        dialect.storage_engine: innodb
  session:
    store-type: jdbc